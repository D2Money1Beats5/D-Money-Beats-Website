export async function onRequestPost({request,env}){try{const body=await request.json();if(!body.email||!body.message)return json({error:'Email and message are required.'},400);if(!env.CONTACT_WEBHOOK)return json({error:'The contact delivery connection is being finalized.'},503);const r=await fetch(env.CONTACT_WEBHOOK,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...body,source:'dmoneybeats.com contact form',created_at:new Date().toISOString()})});if(!r.ok)return json({error:'Unable to send right now.'},502);return json({message:'Request sent.'});}catch(e){return json({error:'Unable to send right now.'},500)}}function json(data,status=200){return new Response(JSON.stringify(data),{status,headers:{'content-type':'application/json','cache-control':'no-store'}})}